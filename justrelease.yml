version.update:
    - json,js
create.artifacts:
        - npm install
        - npm test
publish:
        - github:
            - description=releasenotes.md
            - attachment=lib/index.js
        - npm:
scm
    - commit=${version} has just been released
    - tag=t${version}
